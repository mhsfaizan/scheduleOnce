import { MyFileDirective } from './my-file.directive';

describe('MyFileDirective', () => {
  it('should create an instance', () => {
    const directive = new MyFileDirective();
    expect(directive).toBeTruthy();
  });
});
